<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../style/homePageStyle.css">
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <title>CSV Upload | Home</title>
</head>

<body>
    <header>
        <h1>
            CSV File Uploader and Reader
        </h1>
    </header>


    <section id="upload-section">
        <h4>
            Upload a .CSV file here
        </h4>
        <form action="/csv/upload" method="post" enctype="multipart/form-data">
            <input type="file" name="csvFile" id="">

            <input type="submit" id="upload-submit-btn">


        </form>
    </section>
    <section id="list-section">
        <h3>
            Uploaded Files
        </h3>
        <div id="list-container">
            <% for(let i=0 ; i< csvFiles.length; i++) {%>
                <div class="list-tile">
                    <div class="item-number">
                        <%= i+1 %>
                    </div>
                    <div class="file-name">
                        <%= csvFiles[i].fileName %>
                    </div>
                    <div class="created-at">
                        <%= csvFiles[i].createdAt %>
                    </div>
                    <div class="updated-at">
                        <%= csvFiles[i].updatedAt %>
                    </div>
                    <div class="delete-button">
                        <div onclick="deleteFile('<%=csvFiles[i].id %>')">
                            <span class="material-symbols-outlined">
                                delete
                            </span>
                        </div>
                    </div>
                    <div class="open-button">
                        <a href="/csv/open/<%=csvFiles[i].id %>">
                            <span class="material-symbols-outlined">
                                open_in_new
                            </span>
                        </a>
                    </div>
                    <div>

                    </div>
                </div>
                <% }%>
        </div>
    </section>
</body>

<script>
    async function deleteFile(fileId) {
        fetch(`csv/delete/${fileId}`, { method: 'DELETE' }).then(response => { console.log(response.json()) }).then().catch((err) => {
            console.error(err);
        })
        window.location.href = '/csv';
    }
</script>

</html>